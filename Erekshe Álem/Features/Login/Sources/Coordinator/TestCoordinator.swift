import SwiftUI

public struct TestCoordinator: View {

    public init(dependencies: Dependencies) {

    }

    public var body: some View {
         TestView(.init())
    }
}

extension TestCoordinator {
    public struct Dependencies {
        let
        
        public init(
            
        ) {
            
        }
    }
}
